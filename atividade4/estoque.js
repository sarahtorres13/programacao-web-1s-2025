const itens = [];

export function adicionar(item){
    let itens_valido = true;
    if(isnan(item.id))
    item_valido = false; 
}
    if(item_valido){

        itens.push(item)
    }


export function listar(){

return itens;

}

module.exports = {
    adicionar,
    listar


}